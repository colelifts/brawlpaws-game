{
  "name": "brawlpaws-phase-1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "deploy": "npm test && git push origin main"
  }
}
